const text = "do not change the word, do change the word";
const result = text.replaceAll("do", "dont");
// console.log(result);

const arr = [2, 3, 4, 2];
const checkContain = arr.contains(1);
console.log(checkContain);
